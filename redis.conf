save 900 1       # Save every 900 seconds if at least 1 key changes
save 300 10      # Save every 300 seconds if at least 10 keys change
save 60 10000    # Save every 60 seconds if at least 10000 keys change
appendonly no    # Disable AOF persistence (optional if you rely on snapshots)